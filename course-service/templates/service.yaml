apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-service.fullname" . }}
  labels:
    app: {{ include "course-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "course-service.name" . }}
