apiVersion: v1
kind: Service
metadata:
  name: {{ include "executor-service.fullname" . }}
  labels:
    app: {{ include "executor-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "executor-service.name" . }}
